# GameHub - сайт 

Современный одностраничный сайт для скачивания модов, конфигов и ресурсов для популярных игр.

## 🚀 Возможности

- **Темная тема** с неоновой цветовой схемой (электрик-синий #00eeff и пурпурный #ff00ff)
- **Responsive дизайн** - адаптирован для всех устройств
- **Плавные анимации** и эффекты
- **API для управления файлами** игр
- **Preloader** при загрузке
- **Мобильное меню** навигации
- **Анимации при прокрутке** (fade-in эффекты)
- **Parallax эффекты** для фона

## 🎮 Поддерживаемые игры

- **DDNet** - модификация Teeworlds
- **CS 2** - Counter-Strike 2
- **Minecraft** - песочница с бесконечными возможностями

## 🛠 Технологии

- **Backend**: Python Flask
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Шрифты**: Orbitron (заголовки), Exo 2 (текст)
- **Иконки**: Font Awesome 6

## 📁 Структура проекта

```
project/
├── app.py                 # Flask приложение
├── requirements.txt       # Зависимости Python
├── templates/
│   └── index.html         # Главный шаблон
├── static/
│   ├── css/
│   │   └── style.css      # Стили
│   ├── js/
│   │   └── script.js      # JavaScript
│   ├── downloads/         # Файлы для скачивания
│   │   ├── ddnet/
│   │   ├── cs2/
│   │   └── minecraft/
│   ├── images/            # Иконки и изображения
│   └── favicon.ico        # Фавикон
└── README.md
```

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- pip

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd gamehub
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите приложение:
```bash
python app.py
```

4. Откройте браузер и перейдите по адресу: `http://localhost:5000`

## 📁 Добавление файлов для скачивания

Поместите файлы в соответствующие директории:

- `static/downloads/ddnet/` - файлы для DDNet
- `static/downloads/cs2/` - файлы для CS 2
- `static/downloads/minecraft/` - файлы для Minecraft

Поддерживаемые форматы: `.zip`, `.jar`, `.cfg`, `.png` и другие.

## 🎨 Кастомизация

### Цветовая схема

Измените переменные CSS в `static/css/style.css`:

```css
:root {
    --bg-primary: #0a0a0a;        /* Основной фон */
    --bg-secondary: #1a1a1a;      /* Фон карточек */
    --neon-blue: #00eeff;         /* Электрик-синий */
    --neon-purple: #ff00ff;       /* Пурпурный */
}
```

### Добавление новой игры

1. Добавьте конфигурацию в `app.py` в словарь `GAMES_CONFIG`
2. Создайте секцию в `templates/index.html`
3. Добавьте карточку в секцию "games"
4. Создайте директорию в `static/downloads/`
5. Добавьте иконку в `static/images/`

## 📱 Responsive дизайн

Сайт полностью адаптирован для:
- 📱 Мобильных устройств (320px+)
- 📟 Планшетов (768px+)
- 💻 Настольных компьютеров (1024px+)

## 🎭 Анимации и эффекты

- **Typing animation** для заголовка
- **Fade-in эффекты** при прокрутке
- **Hover эффекты** для интерактивных элементов
- **Parallax** для фоновых элементов
- **Smooth scrolling** между секциями

## 🔧 API endpoints

- `GET /api/files/<game>` - получить список файлов для игры
- `GET /download/<game>/<filename>` - скачать файл
- `GET /api/games` - получить конфигурацию всех игр

## 📄 Лицензия

Проект создан для демонстрации современных веб-технологий.

## 👨‍💻 Автор

**m1ncedPool**

---

*Создано с ❤️ для игрового сообщества*